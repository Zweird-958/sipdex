{
  "name": "fantadex",
  "private": true,
  "packageManager": "pnpm@11.12.0",
  "scripts": {
    "dev": "turbo dev",
    "start": "pnpm --filter @fantadex/backend start",
    "typecheck": "turbo typecheck",
    "lint": "turbo lint",
    "lint:fix": "turbo lint -- --fix",
    "format": "prettier --check .",
    "format:fix": "prettier --write .",
    "knip": "knip",
    "test:unit": "pnpm --filter @fantadex/api test:unit",
    "test:api": "pnpm --filter @fantadex/api test:api",
    "test:integration": "pnpm --filter @fantadex/api test:integration",
    "db:generate": "pnpm --filter @fantadex/api db:generate",
    "db:migrate": "pnpm --filter @fantadex/api db:migrate",
    "db:push": "pnpm --filter @fantadex/api db:push",
    "db:studio": "pnpm --filter @fantadex/api db:studio",
    "seed": "pnpm --filter @fantadex/api seed",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "env-symlink": "ln -sf $PWD/.env ./apps/backend/.env && ln -sf $PWD/.env ./packages/api/.env",
    "prepare": "husky"
  },
  "devDependencies": {
    "@trivago/prettier-plugin-sort-imports": "^6.0.2",
    "husky": "^9.1.7",
    "knip": "^5.66.0",
    "prettier": "^3.9.5",
    "turbo": "^2.5.0"
  }
}
