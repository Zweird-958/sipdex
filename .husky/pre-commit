echo "→ typecheck"
pnpm typecheck || exit 1

echo "→ prettier format check"
pnpm format || exit 1

echo "→ eslint"
pnpm lint || exit 1

echo "→ knip"
pnpm knip || exit 1

echo "→ unit tests"
pnpm test:unit || exit 1

echo "→ api tests"
pnpm test:api || exit 1